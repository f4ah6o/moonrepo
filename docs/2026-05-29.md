# 2026-05-29

## Plan

moonrepo 自体に `rally-rs` / `ral` を任意 helper として組み込む。目的は、親 thread、Codex worker、Claude Code、OpenCode reviewer の間で短い連絡を渡しやすくすること。

作業範囲は `justfile`、`README.md`、`AGENTS.md`、`scripts/codex-task.sh` に限定する。`ral` は task state の正にしない。state は引き続き `.codex/tasks/*.json`、git、GitHub に置く。

## 実施内容

`github.com/f4ah6o/rally-rs` の `main` を確認し、`ral skills` が `0bfd7afd683a820ed6ff3279c4561521c051a9c6` に入っていることを見た。crates.io の `2026.5.0` にはまだ `skills` サブコマンドが無いため、install recipe は GitHub revision pin にする。

追加する recipe は、install と状態確認、skill 文面確認、moonrepo checkout の team 登録、inbox/send/history/team/mode の thin wrapper にする。`just doctor` の必須前提には入れない。
