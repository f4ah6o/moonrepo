# 2026-05-02

## 今日の計画

- `moonrepo` の管理対象に `f4ah6o/papyr.mbt` を追加する。
- 空の `f4ah6o/papyr.mbt` を clone し、`papyr.mbt#1` の Phase 1-2 を始められる MoonBit module を初期化する。
- `NormalizedDocument` v0 契約、decoder、stable diagnostics、最小 CLI、fixture/golden test を作る。

## 作業メモ

- `f4ah6o/papyr#1` は closed、今回の対象は `f4ah6o/papyr.mbt#1`。
- `f4ah6o/papyr.mbt` は empty repo なので、最初の commit 相当の骨組みから作る必要がある。
- `repository.ini` に `f4ah6o/papyr.mbt` を追加して clone した。
- `papyr.mbt` では `moon.mod.json`、`src/core`、`src/cmd/main`、fixture、test を追加した。
- `NormalizedDocument` v0 decoder と stable diagnostics JSON を実装した。
- `moon check`、`moon test`、`moon info`、`just moon-check papyr.mbt`、`just moon-test papyr.mbt` は通った。
- `just doctor` は既存の `scripts/repo-lib.sh` にある `local -A` が Bash 3 系で失敗するため未解消のまま。
