# 2026-05-02

## 今日の計画

- `moonrepo` の管理対象に `f4ah6o/papyr.mbt` を追加する。
- 空の `f4ah6o/papyr.mbt` を clone し、`papyr.mbt#1` の Phase 1-2 を始められる MoonBit module を初期化する。
- `NormalizedDocument` v0 契約、decoder、stable diagnostics、最小 CLI、fixture/golden test を作る。

## 作業メモ

- `f4ah6o/papyr#1` は closed、今回の対象は `f4ah6o/papyr.mbt#1`。
- `f4ah6o/papyr.mbt` は empty repo なので、最初の commit 相当の骨組みから作る必要がある。
- `repository.ini` に `f4ah6o/papyr.mbt` を追加して clone した。
- `papyr.mbt` では `moon.mod.json`、`src/core`、`src/cmd/main`、fixture、test を追加した。
- `NormalizedDocument` v0 decoder と stable diagnostics JSON を実装した。
- `moon check`、`moon test`、`moon info`、`just moon-check papyr.mbt`、`just moon-test papyr.mbt` は通った。
- `just doctor` は既存の `scripts/repo-lib.sh` にある `local -A` が Bash 3 系で失敗するため未解消のまま。
- `papyr.mbt` に `apps/docs` と必要な TypeScript packages を vendor し、`#11` の compatibility fixture を固定した。
- frozen snapshot は `f4ah6o/papyr` の `0b04ac960da9e601c241f4eaed3b28618a10b8a1`。この commit の `documents.json`、`manifest.json`、`search-index.json`、`r2/**` を fixture にした。
- `#12` として decoder の出力 shape を current docs corpus に寄せ、`title`、`meta`、block `id`、heading `level`、rich `table` / `mermaid` / `excalidraw` を扱えるようにした。
- `#13` の first pass として publication projection を追加した。ここでいう projection は、decoded document 群から docs site が読む `manifest.json` 相当を組み立てる処理を指す。
- projection の詰まりどころは sort order だった。upstream TypeScript は `localeCompare()` を使っていて、MoonBit の単純な文字列比較では `BackendAdapter` と `backend につなぐ` のような順序がずれた。
- 対応として `src/core/moon.pkg` の target 分岐を使い、JS backend だけ `localeCompare()` を使う `collation_js.mbt`、それ以外は lexical fallback を使う `collation_fallback.mbt` に分けた。これで frozen `manifest.json` と一致しつつ `moon check` も維持できた。
- 2026-05-02 時点の確認は `moon check` と `moon test --target js`。manifest fixture test まで green。
- `#14` の first pass として docs-build CLI を追加した。ここでの artifact bundle は、`documents.json`、`manifest.json`、`search-index.json`、`r2/**`、`public-data/**` を file path と content の配列として返す machine-readable な出力を指す。
- docs-build は current docs style の `documents.json` envelope と raw markdown directory、caller-supplied `generatedAt` を受ける。`generatedAt` を caller から渡すのは、golden test を wall-clock time から切り離して byte-stable に保つため。
- JSON artifact は JS target で `JSON.stringify(value, null, 2)` 相当の pretty render を使い、non-JS target では buildability を優先して fallback renderer を使うようにした。これで fixture parity と cross-target `moon check` を両立した。
- `#16` の first pass として `apps/docs/scripts/build-content.ts` を JS parse + MoonBit build-docs bridge に差し替えた。Markdown / frontmatter parse は引き続き TypeScript が持ち、publication projection と artifact emission は MoonBit CLI に委譲する形になった。
- bridge は一時 directory に current docs style の `documents.json` と raw markdown files を出して、`moon run src/cmd/main --target native -- build-docs ...` を呼ぶ。返ってきた artifact bundle を app 側の `dist/content` と `src/client/public/__papyr_docs_data` に展開する。
- native target の fallback collation だけだと manifest の publication 順が upstream とずれるので、vendored build script 側で manifest の `publications` を TypeScript の `localeCompare()` 規則に正規化してから書き出すようにした。
- parity 用には `PAPYR_GENERATED_AT` を受けられるようにした。これで fixture 比較では timestamp を pin し、通常 build では現在時刻を使う運用に分けられる。
