# 2026-05-27

## Plan

古い root `worktrees/` に残っていた retained worktree の follow-up issue を片付ける。方針は conservative archive とし、変更や branch を失わないことを優先する。

## 実施内容

`issues/2026-05-16T044019Z-resolve-dirty-retained-worktrees.md` と `issues/2026-05-16T044019Z-resolve-unmerged-retained-worktrees.md` を対象にした。

dirty worktree では、未コミット差分を破棄せずに各 branch 上で `chore: preserve retained worktree changes` として保存した。これは、その時点の作業状態を commit graph に固定することで、worktree directory を消しても `git branch` から復元できるようにするため。

clean だが unmerged な worktree は、branch を消さずに worktree だけを外した。`git worktree remove` は作業 directory と git の worktree 登録を消す操作で、branch 自体を削除する操作ではない。

最後に issue file の checkbox と acceptance criteria を完了状態に更新した。

## 確認

- root `worktrees/` 配下の対象 worktree を `git worktree remove --force` で削除した。
- dirty worktree の変更は local commit として保存した。
- local branch と remote branch は削除していない。
