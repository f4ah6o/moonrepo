# 2026-05-09

## 今日の plan

- `papyr.mbt`、`domainprocessschema.mbt`、`vizprocess.mbt` の Codex App Server 対応 issue を cross-repo convention に揃える。
- 既存 shared repo `f4ah6o/codex-app-server-shared` を App Server client/core の最小 shared vocabulary として整理する。
- 各 repo は `just codex-start <repo> <task-slug>` で専用 worktree を切り、親 thread は moonrepo 側で orchestration する。

## task slug

- `domainprocessschema.mbt`: `app-server-schema-session`
- `vizprocess.mbt`: `app-server-process-workspace`
- `papyr.mbt`: `app-server-authoring-publish`
- `codex-app-server-shared`: `app-server-shared-core`

## 方針メモ

- repo-local issue を source of truth に維持し、今回は GitHub issue へ同期しない。
- shared repo は `client/core` のみを持ち、session envelope、approval flow、notification/resume primitive を扱う。
- repo 固有 snapshot field、tool schema、preview renderer、VS Code extension host、browser demo UI は shared repo に入れない。
- 実装順は `domainprocessschema.mbt` をアンカーにして語彙と approval 境界を固め、その vocabulary を `vizprocess.mbt` と `papyr.mbt` へ展開する。
- `domainprocessschema.mbt` の契約を先に implementation-ready にし、shared repo は domain / vizprocess / papyr の共通 envelope だけを持つ。
- 親 thread は moonrepo 側で orchestration し、repo 内の実装変更は `just codex-start` で作った worktree を所有する worker に任せる。
- `repository.ini` は今回の対象 repo と shared repo を active set に含むものとして扱う。
- `docs/2026-05-08.md` の docs-humanizer / papyr document 改善ログも今回の作業履歴に含めて扱う。

## 着手ログ

- `just codex-start domainprocessschema.mbt app-server-schema-session`
  - worktree: `worktrees/domainprocessschema.mbt-app-server-schema-session-20260509`
  - branch: `codex/app-server-schema-session`
- `just codex-start codex-app-server-shared app-server-shared-core`
  - worktree: `worktrees/codex-app-server-shared-app-server-shared-core-20260509`
  - branch: `codex/app-server-shared-core`
- `domainprocessschema.mbt` worker は App Server schema/runtime session contract を anchor contract として更新し、`moon check` で 0 errors を確認した。
- `codex-app-server-shared` worker は shared envelope/approval/resume test を追加し、`npx -y node@26 --test src/index.test.ts` と `npm run typecheck` を通した。
- `just codex-start vizprocess.mbt app-server-process-workspace`
  - worktree: `worktrees/vizprocess.mbt-app-server-process-workspace-20260509`
  - branch: `codex/app-server-process-workspace`
- `just codex-start papyr.mbt app-server-authoring-publish`
  - worktree: `worktrees/papyr.mbt-app-server-authoring-publish-20260509`
  - branch: `codex/app-server-authoring-publish`
