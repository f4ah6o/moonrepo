# 2026-05-08

## 今日の計画

- `domainprocessschema.mbt` と `vizprocess.mbt` の Cloudflare Workers deploy 導線を管理 repo から叩けるようにする。
- root `justfile` に共通 Cloudflare recipe を追加する。
- `vizprocess.mbt` に browser demo 用の Worker / Wrangler / asset build を追加する。
- preview / production の deploy を実行し、公開 URL をブラウザで確認する。
- `repos/` 配下の実装作業を Codex sub agent 前提の worktree / PR フローへ寄せる入口を moonrepo に追加する。
- `just codex-start` / `just codex-status` / `just codex-pr` と task manifest を整備する。
- Zenn の anti-AI-writing 記事を repo local skill に落とし、`repos/` 配下 document の audit / 改善 workflow を追加する。

## 作業メモ

- root `justfile` に `cloudflare-op-create`、`cloudflare-op-env`、`cloudflare-build`、`cloudflare-dev`、`cloudflare-deploy-preview`、`cloudflare-deploy` を追加した。
- 対応 repo は `domainprocessschema.mbt` と `vizprocess.mbt` のみに固定し、それ以外は即時エラーにした。
- `domainprocessschema.mbt` では既存 Worker 構成を再利用し、production custom domain として `domainprocessschema.f12o.com` を Wrangler route に追加した。
- `vizprocess.mbt` では browser demo をそのまま公開 asset として配る方針にし、新しい API Worker は作らず `env.ASSETS.fetch(request)` だけを返す最小 Worker にした。
- `vizprocess.mbt` に `package.json`、`wrangler.jsonc`、`examples/browser-demo/worker.mjs`、`examples/browser-demo/.env.opz.example`、`scripts/build-cloudflare-demo.mjs` を追加した。
- asset bundle は `_build/cloudflare/browser-demo/` に `index.html`、`prefetch.mjs`、`duckdb-loader.mjs`、`opfs.mjs`、`wasm.js`、`fixtures/datasets/sales.csv` を出す。
- `index.html` 内の `../../_build/js/release/build/wasm/wasm.js` と `../../fixtures/datasets/sales.csv` は deploy 用 bundle で `./wasm.js` と `./fixtures/datasets/sales.csv` に書き換えるようにした。
- 検証では `moon -C repos/vizprocess.mbt build --target js --release` と `node repos/vizprocess.mbt/examples/browser-demo/node-smoke.mjs` を通す。
- `domainprocessschema.mbt` / `vizprocess.mbt` とも production route は top-level ではなく `env.production.routes` に置いた。こうしておくと `preview` deploy が custom domain を奪わず、`workers.dev` だけで確認できる。
- `vizprocess.mbt` の `compatibility_date` は Cloudflare API が future date と見なさないよう `2026-05-07` に合わせた。
- `just cloudflare-build domainprocessschema.mbt`、`just cloudflare-build vizprocess.mbt`、`just cloudflare-deploy-preview <repo>`、`just cloudflare-deploy <repo>` は通った。
- preview URL:
  - `https://domainprocessschema-wasm-demo-preview.f12o.workers.dev`
  - `https://vizprocess-browser-demo-preview.f12o.workers.dev`
- production URL:
  - `https://domainprocessschema.f12o.com`
  - `https://vizprocess.f12o.com`
- headless browser では `vizprocess.f12o.com` の初回 render、DuckDB example、OPFS seed、OPFS example render を確認した。
- `domainprocessschema` は preview / production とも公開ページが開き、`Start runtime session` と `Current view` が見えるところまで確認した。
- `scripts/codex-task.sh` を追加し、`repos/<repo>` の clean clone から `worktrees/<repo>-<task-slug>-<YYYYMMDD>/` を作る `just codex-start` を実装した。
- task manifest は `.codex/tasks/<repo>-<task-slug>.json` に保存し、親 thread が orchestration だけを担当する前提の worker 起動 prompt を出すようにした。
- `just codex-status` では manifest / worktree / branch / dirty / upstream / ahead/behind を確認できるようにした。
- `just codex-pr` では worktree の clean 状態、upstream、`gh` 認証、GitHub branch protection の required checks を確認し、通過時だけ draft PR を作るようにした。
- moonrepo 自身の運用文書として `README.md` と `AGENTS.md` に Codex sub agent 運用ルールを追記した。
- Zenn の <https://zenn.dev/m0370/articles/205c9340a418c3> をもとに `.agents/skills/docs-humanizer/` を追加し、日本語 document 向け anti-AI-writing チェックリストを `references/anti-ai-checklist-ja.md` に整理した。
- `docs-humanizer` には tracked documents を対象にした機械監査 script `audit-docs.sh` と、`codex-start` と組み合わせて docs 専用 worktree を起動する `docs-workflow.sh` を付けた。
- root `justfile` に `just docs-audit <repo>`, `just docs-audit-all`, `just docs-review <repo> <task-slug>` を追加し、`README.md` / `AGENTS.md` に新 workflow を追記した。
- `repos/papyr.mbt` では `README.mbt.md` を docs-humanizer の観点で整理し、repo の役割、参照先、vendored docs workspace の前提、CLI contract、local issue workflow の入口を重複を減らしながら書き直した。
- `repos/papyr.mbt/apps/docs/content/` では `markdown-formatter-ci` 記事、`backend` book、Cloudflare publication stack chapter を見直し、見出し内ダッシュ、太字ラベル箇条書き、冗長な説明を削って文体を素直にした。
- 続けて `getting-started` の install / connect-backend / try-playground、`adapter-cloudflare` book、`official-docs-site` use case も prose 寄りに整理し、導線用の `- ラベル: 説明` 箇条書きを減らした。
- さらに `adapter-airtable`、`adapter-appsheet`、`adapter-zoho-creator`、`adapter-fs`、`adapter-kintone` の chapter も整え、options / 振る舞いの機械的な箇条書きをそのまま読める説明文へ寄せた。
- 続けて `backend/core/editor-ui/search/markdown/editor/preview/cli` と `integrated-editor` / `advanced-playground` / `local-authoring-workflow` の文書も prose 寄りに寄せ、API 紹介章の `- ラベル: 説明` をかなり削った。残件は `issues/` や compatibility spec のように、構造化メタデータや仕様列挙そのものが目的の文書が中心。
