# 2026-02-05

## 今日の計画
- repository.ini の仕様を決める（1行1リポジトリ、空行と # を無視）
- `gh repo list` から初期生成できるようにする（.mbt でフィルター、コメントアウト）
- justfile に clone / pull / deps 更新コマンドを追加する
- turbo 風に moon fmt/check/build/clean を一括実行できるようにする
- turbo 風に moon test も一括/個別で実行できるようにする
- AGENTS.md にワークフローを追記する

## 実装計画
- 変更するファイル: `justfile`, `repository.ini`, `AGENTS.md`, `docs/2026-02-05.md`
- 変更内容:
  - `justfile`: `gh repo list` から `repository.ini` を生成するレシピと、`moon-dst` 用レシピを追加
  - `justfile`: 全 repos/個別 repo の `moon fmt/check/build/clean` レシピを追加
  - `justfile`: 全 repos/個別 repo の `moon test` レシピを追加
  - `repository.ini`: リポジトリ一覧の入力フォーマットをコメントで明記
  - `AGENTS.md`: 実行手順（init / clone / pull / deps）を追記

## 作業ログ
- `justfile` に `repos-init`（.mbt でフィルター）を追加し、生成結果をコメントアウトで出力するようにした
- `justfile` に `repos-clone` / `repos-pull` / `repos-sync` と `moon-dst` 用レシピを追加
- `justfile` に `moon fmt/check/build/clean` を全 repos/個別 repo 向けに追加
- `justfile` に `moon test` を全 repos/個別 repo 向けに追加
- `repository.ini` を作成し、1行 `owner/repo` 形式で書くルールを明記
- `AGENTS.md` に依存更新のワークフローを追記

## 学び
- `moon-dst` は MoonBit の依存更新をまとめて実行する CLI（`scan` / `apply` / `just`）で、`--root` で対象ディレクトリをまとめて指定できる
- `gh repo list` は `--json name --jq` を使うとフィルタ付きの一覧を簡単に生成できる

## スクリーンショット
- まだ取得していない（必要なら次回追加）
