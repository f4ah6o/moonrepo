# 2026-05-30

## Plan

target repo の `.wt/main` を default branch の baseline checkout として固定する。
これは、作業ごとに branch を切ったり tracked file を変更したりする場所を `.wt/main` から分離し、`target-repos/<repo>.git/.wt/` 配下の専用 worktree に寄せるため。

変更対象は `scripts/repo-lib.sh`、`scripts/codex-task.sh`、`scripts/moonbit-bump.sh`、`justfile`、`README.md`、`AGENTS.md`、`docs/DEVELOPMENT_GUIDE.md` にする。
`repo-lib` に `.wt/main` の baseline 検証と task worktree 作成 helper を追加し、`codex-start`、`refactor`、依存更新、MoonBit toolchain 更新の入口から `.wt/main` 直接変更を避ける。

検証は shell script の構文確認、`just --list`、新しい baseline check、必要に応じた dry-run で行う。

## papyr.mbt issue #84

`f4ah6o/papyr.mbt#84` は、papyr.mbt を軽量な Markdown / 公開基盤から、検索・権限・履歴・テンプレート・連携を備えたドキュメント運用基盤へ近づけるための比較調査 issue。
今回の実装では、issue 全体を一度に大きく作るのではなく、まず repo の現状に合わせて最初の実装単位を切り出す。

変更対象は `target-repos/papyr.mbt.git/.wt/codex/issue-84-doc-tool-foundation/` 配下に限定する。
最初に既存の MoonBit package、schema、CLI、docs、tests を読み、README 付き階層モデル・検索・履歴・ACL のうち、既存構造に自然に入る最小単位を選ぶ。
実装後は変更範囲に合わせて `moon fmt`、`moon check`、`moon test`、必要なら JS/TypeScript 側の check を実行する。
